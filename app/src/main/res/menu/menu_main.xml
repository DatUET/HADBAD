<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item android:id="@+id/it_logout"
        android:title="Logout"/>

    <item android:id="@+id/it_setting"
        android:title="Setting"/>

    <item android:id="@+id/it_search"
        android:title="Search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="always"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <item android:id="@+id/it_add_post"
        android:title="Add Post"
        android:icon="@drawable/ic_add_white"
        app:showAsAction="always"/>

    <item android:id="@+id/it_call_phone"
        android:title="Call"
        android:icon="@drawable/ic_call_phone"
        app:showAsAction="always"
        android:visible="false"/>

</menu>